# IMPORTANT: Secrets must be pre-provisioned as Kubernetes Secrets in each mesh
# namespace before deploying. Create a secret named <fullnameOverride>-secrets in
# each namespace (ak-mesh-main, ak-mesh-peer1, ak-mesh-peer2, ak-mesh-peer3) with
# the required keys (JWT_SECRET, PEER_API_KEY, etc.) before applying this
# ApplicationSet. Use ExternalSecrets or kubectl create secret to provision them.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: artifact-keeper-mesh-test
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: mesh-main
            namespace: ak-mesh-main
            fullnameOverride: ak-main
            valuesFile: values-mesh-main.yaml
            peerName: mesh-main
            peerEndpoint: http://ak-main-backend.ak-mesh-main.svc.cluster.local:8080
          - name: mesh-peer-1
            namespace: ak-mesh-peer1
            fullnameOverride: ak-peer1
            valuesFile: values-mesh-peer.yaml
            peerName: mesh-peer-1
            peerEndpoint: http://ak-peer1-backend.ak-mesh-peer1.svc.cluster.local:8080
          - name: mesh-peer-2
            namespace: ak-mesh-peer2
            fullnameOverride: ak-peer2
            valuesFile: values-mesh-peer.yaml
            peerName: mesh-peer-2
            peerEndpoint: http://ak-peer2-backend.ak-mesh-peer2.svc.cluster.local:8080
          - name: mesh-peer-3
            namespace: ak-mesh-peer3
            fullnameOverride: ak-peer3
            valuesFile: values-mesh-peer.yaml
            peerName: mesh-peer-3
            peerEndpoint: http://ak-peer3-backend.ak-mesh-peer3.svc.cluster.local:8080
  template:
    metadata:
      name: "ak-{{name}}"
      labels:
        app.kubernetes.io/part-of: artifact-keeper
        environment: mesh-test
        mesh-role: "{{name}}"
      annotations:
        argocd-image-updater.argoproj.io/image-list: backend=ghcr.io/artifact-keeper/artifact-keeper-backend:dev, web=ghcr.io/artifact-keeper/artifact-keeper-web:dev
        argocd-image-updater.argoproj.io/backend.update-strategy: digest
        argocd-image-updater.argoproj.io/backend.helm.image-name: backend.image.repository
        argocd-image-updater.argoproj.io/backend.helm.image-tag: backend.image.tag
        argocd-image-updater.argoproj.io/web.update-strategy: digest
        argocd-image-updater.argoproj.io/web.helm.image-name: web.image.repository
        argocd-image-updater.argoproj.io/web.helm.image-tag: web.image.tag
        argocd-image-updater.argoproj.io/write-back-method: argocd
    spec:
      project: artifact-keeper
      source:
        repoURL: https://github.com/artifact-keeper/artifact-keeper-iac.git
        targetRevision: HEAD
        path: charts/artifact-keeper
        helm:
          valueFiles:
            - "{{valuesFile}}"
          parameters:
            - name: fullnameOverride
              value: "{{fullnameOverride}}"
            - name: backend.env.PEER_INSTANCE_NAME
              value: "{{peerName}}"
            - name: backend.env.PEER_PUBLIC_ENDPOINT
              value: "{{peerEndpoint}}"
            # secrets.jwtSecret and backend.env.PEER_API_KEY must be provided via
            # ExternalSecrets or pre-created Kubernetes Secrets in each mesh namespace.
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{namespace}}"
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        retry:
          limit: 3
          backoff:
            duration: 30s
            factor: 2
            maxDuration: 5m
